
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

#autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
#monitor=,1920x1200@60,0x0,1
monitor=,1920x1200@60,0x0,1.25
#monitor=,1920x1200@60,0x0,1.5
#monitor=,1280x800@60,0x0,1
#suggested scale values: 1, 1.25(recommended), 1.5, 1.666667, 2


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = alacritty
$fileManager = nemo
$menu = wofi --show drun -I -W 425 -m
$lock = hyprlock
$browser = brave --enable-features=UseOzonePlatform,WaylandWindowDecorations --ozone-platform=wayland
$browser_incognito = brave --incognito
	#$browser = firefox
	#$music = amberol
$music = rhythmbox
	#$music = lollypop
	#$online-music = spotify
	#$online-music = apple-music --enable-features=UseOzonePlatform,WaylandWindowDecorations --ozone-platform=wayland
	#$online-music = cider --enable-features=UseOzonePlatform,WaylandWindowDecorations --ozone-platform=wayland
$online-music = brave --app=https://music.apple.com/in/home --class=WebApp-AppleMusicWA7121 --name=WebApp-AppleMusicWA7121
$bar = waybar
	#$dock = nwg-dock-hyprland -i 33 -d -hd 0 -p left -c 'wofi --show drun' -ico edit-find
	#$wob = /tmp/wobpipe
$init = ~/.config/hypr/sesh-init.sh
$low-battery-indicator = ~/.config/hypr/bat-mon-hypr.sh
	#$workspace-overview = hyprctl plugin load ~/.config/hypr/Hyprspace-main/Hyprspace.so
$osd = syshud -M audio_out & syshud -M brightness
$blue-light-filter = wlsunset -T 6500 -t 3000 -S 06:30 -s 19:30 -d 1800
#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
# exec-once = swaybg -i $wallpaper
# exec-once = waybar
# exec-once = waypaper --restore
# exec-once = $workspace-overview
#exec-once = sleep 5 && $bar 
exec-once = systemctl --user start hyprpolkitagent
exec-once = hyprpm reload
exec-once = $low-battery-indicator
exec-once = $init	#starts the swww daemon, waybar, nwg-dock-hyprland and sets the initial gtk theme/system theme
#exec-once = swaync	#starts the swaync notification centre
#exec-once = $dock	#starts the nwg-dock-hyprland
exec-once = $osd
exec-once = playerctld daemon
exec-once = exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = nm-applet 
exec-once = $menu
exec-once = $blue-light-filter

#autostart(custom)
exec-once = [workspace 1 silent] $terminal
exec-once = [workspace 2 silent] $browser
exec-once = [workspace 3 silent] $fileManager
exec-once = [workspace special:music silent] $music
#exec-once = [workspace special:music silent] $online-music
#exec-once = [workspace special:magic silent] syncthing-gtk

############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = XCURSOR_THEME, Bibata_Ghost
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland

##############mycolors by sampath#########
source = ~/.config/hypr/myhyprcolors.conf
source = ~/.config/hypr/myhyprsettings.conf
#################################

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 5
    gaps_in = $gaps.in
    gaps_out = 10
    gaps_out = $gaps.out

    #border_size = 2
    border_size = 3

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(ffffffff)
    col.active_border = $act.border
    col.inactive_border = rgba(777777ff)
    col.inactive_border = $inact.border

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true 

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
   #rounding = 10
    rounding = $round

    # Change transparency of focused and unfocused windows
   #active_opacity = 0.85
   #inactive_opacity = 0.75
    active_opacity = $act.opacity
    inactive_opacity = $inact.opacity
    
    shadow {
   	 enabled = true
   	 range = 30
   	 render_power = 3
   	 #col.shadow = rgba(000000ff)
   	 color = $shadow.color
   	 scale = 1.0
   	 offset = 5 5

   	 #shadow_range = 0
   	 #shadow_render_power = 0 
   	 #shadow_offset = 25 25

	 range = $shadow.range
	 render_power = $shadow.render_power
	 offset = $shadow.offset
    }
    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size =  2
        passes = 3
	noise = 0.08
        
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true
    #enabled = false

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = myBeziercust, 0.05, 0.9, 0.1, 1
    bezier = linear, 0, 0, 1, 1

    #animation = windows, 1, 7, myBezier
    animation = windows, 1, 7, myBeziercust
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    #animation = workspaces, 1, 6, default
    animation = workspaces, 1, 6, default, slidevert 
    animation = specialWorkspace, 1, 6, default
    animation = layersOut, 1, 4, linear
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this to be true
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
   # force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
   # disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0.35 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    #workspace_swipe = false
    #i have done some work under this comment
    workspace_swipe = true
    workspace_swipe_invert = false
    workspace_swipe_fingers = 3
    workspace_swipe_min_fingers = false
    workspace_swipe_distance = 99
    workspace_swipe_cancel_ratio = 0
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, C, exec, $terminal
bind = $mainMod, K, killactive,
bind = $mainMod SHIFT, K, exec, hyprctl activewindow |grep pid |tr -d 'pid:'|xargs kill -9
#bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, F, exec, $menu
#bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, T, togglesplit, # dwindle
bind = $mainMod, G, exec, $lock
bind = $mainMod, H, exec, $browser 
bind = $mainMod SHIFT, H, exec, $browser_incognito
bind = $mainMod, O, exec, $music
#bind = $mainMod, Z, exec, hyprshade toggle blue-light-filter
bind = $mainMod, Z, exec, killall -SIGUSR1 wlsunset

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

bind = $mainMod, A, togglespecialworkspace, music
bind = $mainMod SHIFT, A, movetoworkspace, special:music

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod ALT, mouse:272, resizewindow
bindm = $mainMod, mouse:273, resizewindow

# keybindings by me!!! all the way down till next section #
# theme switching!
#bind = $mainMod SHIFT, R, exec, bash ~/.config/hypr/switch.sh
bind = $mainMod, R, exec, ~/.config/hypr/refresh.sh
bind = $mainMod SHIFT, F, exec, ~/.config/hypr/theme-menu.sh
bind = $mainMod, D, exec, nwg-dock-hyprland
bind = $mainMod, Y, exec, killall -SIGUSR1 waybar #toggles the visibility of waybar
#MEDIA
bindl = $mainMod, P, exec, playerctl play-pause
bindl = $mainMod SHIFT, P, exec, playerctl stop
bindl = $mainMod, bracketright, exec, playerctl next
bindl = $mainMod, bracketleft, exec, playerctl previous
#browsers
bind = ALT SHIFT, P, exec, playerctl -i Lollypop,spotify play-pause
bind = ALT SHIFT, bracketright, exec, playerctl -i Lollypop,spotify next
bind = ALT SHIFT, bracketleft, exec, playerctl -i Lollypop,spotify previous
#spotify
bind = CTRL ALT, P, exec, dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.PlayPause
bind = CTRL ALT, bracketright, exec, dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Next
bind = CTRL ALT, bracketleft, exec, dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Previous
#lollypop
bind = CTRL SHIFT, P, exec, dbus-send --print-reply --type=method_call --dest='org.gnome.Lollypop' /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.PlayPause
bind = CTRL SHIFT, bracketright, exec, dbus-send --print-reply --type=method_call --dest='org.gnome.Lollypop' /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Next
bind = CTRL SHIFT, bracketleft, exec, dbus-send --print-reply --type=method_call --dest='org.gnome.Lollypop' /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Previous

#notifications
bind = $mainMod, N, exec, swaync-client -t -sw
$date = notify-send "$(date '+%A %b %d %I:%M %p')" -i appointment -e -t 3000
bind = $mainMod ALT, T, exec, $date
$battery_stat = notify-send "Battery: $(cat /sys/class/power_supply/BAT0/capacity)% $(cat /sys/class/power_supply/BAT0/status)" -i battery -e -t 3000
bind = $mainMod ALT, Y, exec, $battery_stat

#keyboard shortcuts extended
bindl = $mainMod, F8, exec, pamixer -t
bindel = $mainMod, F9, exec, pamixer -d 5 
bindel = $mainMod, F10, exec, pamixer -i 5 
bindel = $mainMod, F11, exec, backlight_control -3 
bindel = $mainMod, F12, exec, backlight_control +3  

#keyboard shortcuts
bindel =, XF86AudioRaiseVolume, exec, pamixer -i 5 
bindel =, XF86AudioLowerVolume, exec, pamixer -d 5 
bindl =, XF86AudioMute, exec, pamixer -t 
bindl =, XF86AudioPlay, exec, playerctl play-pause
bindl =, XF86AudioNext, exec, playerctl next
bindl =, XF86AudioPrev, exec, playerctl previous
bindel =, XF86MonBrightnessUp, exec, backlight_control +3  
bindel =, XF86MonBrightnessDown, exec, backlight_control -3 
bind =, XF86Calculator, exec, galculator
bindl =, Print, exec, grimblast --notify save screen

#window manipulations
bind = $mainMod, Q, fullscreen, 1 
bind = $mainMod SHIFT, Q, fullscreen, 0 
bind = $mainMod CTRL, Q, fullscreenstate, 0 2
bind = $mainMod, I, pin,
bind = $mainMod SHIFT, left, swapwindow, l 
bind = $mainMod SHIFT, right, swapwindow, r 
bind = $mainMod SHIFT, up, swapwindow, u
bind = $mainMod SHIFT, down, swapwindow, d 
binde = $mainMod CTRL, left, resizeactive, -10 0
binde = $mainMod CTRL, right, resizeactive, 10 0
binde = $mainMod CTRL, up, resizeactive, 0 -10
binde = $mainMod CTRL, down, resizeactive, 0 10
binde = $mainMod ALT, left, moveactive, -10 0
binde = $mainMod ALT, right, moveactive, 10 0
binde = $mainMod ALT, up, moveactive, 0 -10
binde = $mainMod ALT, down, moveactive, 0 10

###config for Hyprexpo plugin (workspace overview)###

plugin {
    hyprexpo {
        columns = 3
        gap_size = 5
        bg_col = rgb(111111)
        #workspace_method = center current # [center/first] [workspace] e.g. first 1 or center m+1
        workspace_method = first 1 # [center/first] [workspace] e.g. first 1 or center m+1

        enable_gesture = true # laptop touchpad
        gesture_fingers = 4  # 3 or 4
        gesture_distance = 300 # how far is the "max"
        gesture_positive = true # positive = swipe down. Negative = swipe up.
        #gesture_positive = false # positive = swipe down. Negative = swipe up.
    }
    
    hyprwinwrap {
        # class is an EXACT match and NOT a regex!
        # class = mpv
    }
    
    hyprbars {
        # example config
        bar_height = 20
	bar_color = $act.border
	bar_precedence_over_border = true
	bar_button_padding = 10

        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        #hyprbars-button = rgb(ff4040), 10, , hyprctl dispatch fullscreen 0
        hyprbars-button = $act.border, 10, 󰖭, hyprctl dispatch killactive
        hyprbars-button = $act.border, 10, -
        hyprbars-button = $act.border, 10, , hyprctl dispatch fullscreen 1
        hyprbars-button = $act.border, 10, 󱇙, hyprctl dispatch togglefloating
    }
}


#bind = $mainMod, W, overview:toggle
#bind = $mainMod, W, hyprexpo:expo, toggle
bind = $mainMod, U, workspaceopt, allfloat
#bind = $mainMod, B, exec, hyprpm enable hyprbars
#bind = $mainMod SHIFT, B, exec, hyprpm disable hyprbars
bind = ALT, TAB, cyclenext
bind = ALT, TAB, bringactivetotop
bind = CTRL ALT, left, workspace, e-1
bind = CTRL ALT, right, workspace, e+1
bind = $mainMod ALT, F4, exec, shutdown -h now



##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

#blurring waybar 
layerrule = blur,waybar
#layerrule = blur,swaync-control-center
#layerrule = ignorezero, swaync-notification-window
layerrule = animation slide left, swaync-control-center
#layerrule = dimaround,swaync
layerrule = blur, wofi
#layerrule = dimaround, swaync-control-center

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
windowrulev2 = maximize,initialTitle:(LibreOffice*)
windowrulev2 = fullscreen,initialClass:(org.gnome.Evince)
windowrulev2 = fullscreen,class:(ristretto)
windowrulev2 = fullscreen,initialTitle:(Apple TV*)
#windowrulev2 = float, initialClass: geany
windowrulev2 = float, title: (geany*)
windowrulev2 = float, class: org.gnome.Terminal
windowrulev2 = float, class:(galculator)
windowrulev2 = float, workspace:-97
windowrulev2 = workspace special:magic, title:(xmessage)

windowrulev2 = opacity 0.0 override, class:^(xwaylandvideobridge)$
windowrulev2 = noanim, class:^(xwaylandvideobridge)$
windowrulev2 = noinitialfocus, class:^(xwaylandvideobridge)$
windowrulev2 = maxsize 1 1, class:^(xwaylandvideobridge)$
windowrulev2 = noblur, class:^(xwaylandvideobridge)$

windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.
